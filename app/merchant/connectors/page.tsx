import { connectorCatalog, shopifyPreviewStore } from "../../../lib/connectors";
import { Badge, MerchantShell, PageIntro } from "../merchant-ui";
import ConnectorStatus from "./ConnectorStatus";
import SalespersonManager from "./SalespersonManager";

export default function ConnectorsPage() {
  return <MerchantShell active="storefront" title="Storefront" description="The live Shopify destination, AI salesperson, and connected commerce rails behind every shopper moment."><PageIntro eyebrow="Connected storefront" title="One live destination for every customer conversation." text="Shopify remains the shopper surface. AgentFlow keeps the connection, catalogue, policy, salesperson, and payment state legible for your team." action={<a className="button button-dark" href={shopifyPreviewStore.url} target="_blank" rel="noreferrer">Open live store <span>↗</span></a>} /><ConnectorStatus /><SalespersonManager /><div className="connector-grid-new">{connectorCatalog.map((connector) => <article className="workspace-card connector-card-new" key={connector.name}><div className="connector-card-top"><span className="connector-large-icon">{connector.icon}</span><Badge tone={connector.tone}>{connector.status}</Badge></div><h3>{connector.name}</h3><p>{connector.label}</p><small>{connector.name === "Shopify" ? shopifyPreviewStore.detail : "Safe demo adapter · no production credentials"}</small><button className="button button-light button-full" type="button">Open settings <span>↗</span></button></article>)}</div><section className="shopify-connection"><div className="shopify-connection-symbol">S</div><div><span className="section-label">Shopper destination</span><h3>{shopifyPreviewStore.name}</h3><p>{shopifyPreviewStore.status} · {shopifyPreviewStore.url.replace("https://", "")}</p></div><a className="button button-dark" href={shopifyPreviewStore.url} target="_blank" rel="noreferrer">Open storefront <span>↗</span></a></section><p className="connector-note"><span>⌁</span>Capabilities come from connectors; commercial authority stays in the published policy runtime. The shopper experience lives on Shopify.</p></MerchantShell>;
}
